package com.bayzdelivery.utils;

/**
 * Enum type representing the possible roles a person can have in the system.
 * <p>
 * This enum is used to define and distinguish between different user roles
 * within the application, such as customers, delivery personnel, and administrators.
 */
public enum PersonRole {
    CUSTOMER,
    DELIVERY_MAN,
    ADMIN
}
